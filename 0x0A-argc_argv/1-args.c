#include <stdio.h>

/**
 * main - a function that print th num of argument
 * @argc: integer
 * @argv: thus is pointer
 * Return: 0 or 1
*/

int main(int argc, char *argv[] _attribute_((unsed))
		{
		printf("%d\n", argc - 1);
		return (0);
		}
