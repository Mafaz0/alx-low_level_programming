#include <stdio>

/**
 * main - Entry point
 *
 * Description: print all single digit number
 *
 * Return: always 0
 */

int main(void)
{
	int digit;

	for (digit = 0; i <= 9; i++)
	{
		printf("%i", digit)
	}
	printf("\n")
	return (0);
}

